import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class TreeSearchTest {

//    @Test
//    void inOrderWalk() {
//        ArrayList<Integer> expectedPath = new ArrayList<>(Arrays.asList(3, 2, 1));
//
//
//        BinaryNode<Integer> bn = new BinaryNode<>(1);
//
//        BinaryNode<Integer> root = bn.generateTree(expectedPath);
//
//        ArrayList<Integer> givenPath = new ArrayList<>();
////        BinaryNode<Integer> root = new BinaryNode<>(1);
////        root.left = new BinaryNode<>(2);
////        root.right = new BinaryNode<>(3);
////
//        TreeSearch treeSearch = new TreeSearch();
//        treeSearch.inOrderWalk(root,givenPath);
//
//        assertArrayEquals(expectedPath.toArray(), givenPath.toArray());
//    }
}